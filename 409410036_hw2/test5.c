#include <stdio.h>
int sum(int a, int b){return a+b;}
int g = 2-2-5*8;
int main(){
    int a=0,b=10;
    a += ++a+-a;
    a *= a && (a-8/4-(2+6)%2);
    a |= b%sum(sum(--a,b-5*a*10%2),1);
    sum(1,5);
    sum(1,a);
    sum(1-1,a-a);
    sum(2+2*2,a+=a+1);
    5/a++;
    (a++)+a;
    a+++a--;
    a++-++a;
    a+-a;
    ++a+a++;
    (1+2)*3;
    sum(sum(2,a+=2/2),1);
    a = 10;
    while(a|b){
        for(;b;b--){break;}
            
        a/=2;
        continue;
    }
    return 0;
}