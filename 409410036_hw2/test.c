#include <stdio.h>
long power(int a, int b)
{	
	int c = 0, i = 1;
	for( ;i<=b;)
	{
		a*=b;
		i++;
	}
	return b;
}
int sun(int a, int b)
{
	return a+b;
}
void main(){	
	int a = 2, b = 4/a;
	float c = -0.1;
	int c = sum(a,b);
	
	while( 0 || 2-1 && sum(0, sum(0, 1)) )
	{
		int i = 2;
		if (c && a && sum(a,b))
			{c = a;}
		else if (2*0)
			{c *= power(a,c);}
		else 
		{
			c -=1;
			break;
		}
		
	}
	printf("%dhijkl",c);
}
